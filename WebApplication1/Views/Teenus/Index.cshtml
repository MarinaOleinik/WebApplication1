@model IEnumerable<Teenus>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nimetus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hind)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kestvus)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nimetus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Hind)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Kestvus)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TeenusID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TeenusID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TeenusID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
